const path = require('path')
const { loadTestCasesFromYamlFile } = require('../../common/utils');
const { read } = require('./solution');

describe.skip('Read N Characters Given Read4', () => {
  test.each([])('Test Case %#', () => {
  });
});
